services:
  db:
    ports:
      - "5432:5432"
  web:
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"

networks:
  proxy-network:
    external: false
